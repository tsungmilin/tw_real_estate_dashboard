# Dashboard 專案整理紀錄

> 此文件保留 2026-08-25 的歷史整理紀錄。最新狀態請見 [`docs/project-status.md`](docs/project-status.md)。

更新日期：2026-08-25

## 1. 專案目的與目前階段

本專案正在整理中古屋實價登錄原始資料，目標是建立可重現的清理流程、明確的欄位規則，以及後續可供分析或匯入資料庫的 canonical dataset。

目前已完成全資料的分批 profiling 與主要清理邏輯驗證；尚未產出正式 cleaned dataset，也尚未定稿 schema、資料字典與清理程式。原始 `.dta` 保持不變。

## 2. 資料概況

- 原始檔：`data/raw/house_preowned_data_2.0.dta`
- 大小：14,682,511,713 bytes（約 14 GB）
- 筆數：4,380,208
- 處理方式：每批 100,000 筆讀取，避免整份資料載入記憶體
- 目前預計保留的交易類型：`房地(土地+建物)`、`房地(土地+建物)+車位`、`土地`
- 上述三類共 4,295,141 筆（98.06%）；其餘為 `車位` 61,462 筆、`建物` 23,603 筆及空白 2 筆

## 3. 已完成的工作

| 階段 | 內容 | 主要產物 |
|---|---|---|
| 01 | 檢查 Stata 欄位、型別、標籤與樣本 | `01_inspect_stata.py` |
| 02 | 驗證識別碼、交易日期、土地面積欄位 | `key_fields_validation.csv` |
| 03 | 深入檢查年份、非法日期與土地面積落差 | `anomaly_summary.csv`、`year_distribution.csv`、`land_mismatch_by_land5.csv` |
| 04 | Profiling 交易類型、交易筆數、樓層、建物型態與用途 | `selected_fields_*.csv` |
| 05–06 | Profiling 價格、面積與車位計價公式 | `price_*.csv`、`parking_*.csv` |
| 07 | 檢查格局、建物欄位、完工日期與備註 | `remaining_house_*.csv`、`note_*.csv` |
| 08 | 補齊識別碼唯一性、建物面積與其他分類欄位 | `no_global_uniqueness.csv`、`building_area_*.csv`、`remaining_category_*.csv` |
| 09 | 檢查短位數完工日期及「完工晚於交易」案例 | `completion_*.csv` |

## 4. 已確認的資料特性

### 識別碼與交易日期

- `no` 無缺漏、無空白，4,380,208 筆皆為全域唯一，可作為原始資料主鍵候選。
- `year` 與 `trans_y` 在可比較資料中完全一致，後續不必同時保留兩個同義欄位。
- `year` 有 105 筆缺漏；4,369,855 筆落在民國 101–113 年；另有 10,241 筆小於 101、7 筆大於 113，不能直接視為正常交易年。
- `trans_m` 沒有超出 1–12；`trans_d` 有 130 筆超出 1–31，需隔離或設為缺值後再組日期。

### 土地面積

- `landtotarea` 與 `landtransarea1`–`5` 合計的整體一致率為 96.08%。
- `landtransarea5` 無值時幾乎完全一致；有值的 215,011 筆中，170,414 筆（79.26%）為 `landtotarea` 大於五欄合計。
- 結果強烈支持「交易包含超過五筆土地、明細欄位被截斷」的假設。因此不能用前五筆明細合計覆蓋 `landtotarea`；兩者應分別保留語意並加上 mismatch flag。

### 價格、面積與車位

- `h_price_pin ≈ h_price_m2 × 3.305785`：4,258,995 筆可比較資料中僅 9 筆不一致。
- 無車位房地交易的單價公式吻合率為 98.79%，仍需保留不吻合旗標。
- 含車位交易不能套用單一公式：
  - 車位價格、面積皆大於 0：636,569 筆（47.75%）；同時扣除價與面積的公式吻合率 98.57%。
  - 價格、面積皆為 0：335,609 筆（25.17%）；不調整公式吻合率 93.97%。
  - 僅面積大於 0：327,415 筆（24.56%）；不調整公式吻合率 97.18%。
  - 僅價格大於 0：33,543 筆（2.52%）；不調整公式吻合率 92.06%。
- 價格與面積欄位沒有負值，但有零值與極端最大值，正式清理時需用旗標處理，不能只靠刪除。

### 建物、格局與分類欄位

- 房地交易共 3,146,530 筆。建物面積欄位無缺漏，但 `trans_size` 有 53 筆為 0，`mainbuilding_area` 有 52,071 筆為 0。
- `mainbuilding_area + affbuilding_area <= trans_size` 成立於 3,144,918 筆；加上 `balcony_area` 後成立於 3,142,725 筆。面積元件的定義可能重疊，尚不能直接相加重建總面積。
- `room`、`hall`、`bath` 無缺漏，但最大值分別為 784、322、394,000，顯示需要異常值規則。
- `elevator` 在房地交易中全數缺漏；`nonurban_categ` 在保留交易中全數缺漏，原則上應排除，除非另有來源可補。
- `story` 缺漏率 0.26%；`parking_type` 空白率 0.05%；`usage_type` 空白率 20.31%；`nonurban_type` 空白率 80.06%。後兩者可能是條件式不適用，需依都市／非都市情境判讀，不能一律當成錯誤。

### 完工日期與備註

- `date_complete` 在房地交易中缺漏 65,775 筆（2.09%），另有 32 筆為 0、5 筆為負值。
- 3,079,661 筆可按民國年月日格式解析，其中 3,076,864 筆為有效日曆日期；短位數與無效日期需另行處理。
- 494,884 筆完工年月晚於交易年月。此群以含車位交易、新式大樓及有備註資料為主，可能包含預售／新成屋情境，不應直接判為錯誤或刪除。
- 保留交易中有 1,498,703 筆非空白備註（34.89%）；`note_yn` 與備註是否空白完全一致。
- 常見備註主題包括增建或未登記（446,076 筆）、關係人交易（282,759 筆）、裝修／家具（32,146 筆）等。這些關鍵字分類目前只是 profiling heuristic，尚非正式清理規則。

## 5. 尚未完成／待決策

1. 定稿保留欄位、欄名、型別、nullable 規則與資料字典。
2. 明確定義交易日期、完工日期、土地明細截斷、車位計價及數值極端值的清理規則。
3. 將異常值優先轉為可追蹤的 quality flags；只有語意確定無效時才刪除。
4. 實作由 raw 到 `data/processed/` 的正式 cleaning pipeline，並輸出處理前後筆數與規則命中數。
5. 建立自動 QA：主鍵唯一、日期合法、交易類型、價格／面積範圍、公式一致性與欄位完成率。
6. 補上可重現環境設定。專案目前沒有 `requirements.txt` 或 lock file，系統預設 Python 也尚未安裝 `pandas`。
7. 整理版本控制：目前只有初始 `.gitignore` commit；`src/`、`profiling_output/` 與新的 `.gitignore` 修改都尚未 commit。
8. 將誤放在 `profiling_output/summary/` 的 `06_validate_parking_price_logic.py` 移回 `src/profiling/`；`profiling_output/private/` 保持忽略，避免提交逐筆異常資料。

## 6. 建議的下一個里程碑

先建立 `cleaning_spec.md` 與 canonical schema，逐欄寫明「保留／排除、轉型、缺值、異常 flag」；確認後再實作單一正式清理程式。第一版 processed dataset 應同時附 audit summary，讓每條規則都能回溯到影響筆數。
